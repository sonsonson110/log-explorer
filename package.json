{
  "name": "log-explorer-root",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "apps/web",
    "apps/server-node",
    "packages/contracts"
  ],
  "scripts": {
    "build": "npm run build --workspaces --if-present",
    "typecheck": "npm run typecheck --workspaces --if-present"
  }
}
